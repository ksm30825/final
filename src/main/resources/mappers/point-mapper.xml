<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="Payment">
	<resultMap type="com.kh.ti.point.model.vo.Payment" id="paymentResultSet">
		<id property="paymentId" column="PAYMENT_ID"/>
		
		<result property="tid" column="TID"/>
		<result property="memberId" column="MEMBER_ID"/>
		<result property="payAmount" column="PAY_AMOUNT"/>
		<result property="paymentDate" column="PAYMENT_DATE"/>
	</resultMap>
	<resultMap type="com.kh.ti.point.model.vo.ReservePoint" id="reserveResultSet">
		<id property="pointId" column="POINT_ID"/>
		
		<result property="reservePoint" column="RESERVE_POINT"/>
		<result property="reserveDate" column="RESERVE_DATE"/>
		<result property="memberId" column="MEMBER_ID"/>
		<result property="reserveType" column="RESERVE_TYPE"/>
		<result property="reviewId" column="REVIEW_ID"/>
		<result property="spotReviewId" column="SPOT_REVIEW_ID"/>
		<result property="trvId" column="TRV_ID"/>
	</resultMap>
	<insert id="insertPay" parameterType="Payment">
		INSERT INTO PAYMENT
		VALUES(PAYMENT_SEQ.NEXTVAL, #{tid}, #{memberId}, #{payAmount}, #{paymentDate})
	</insert>	
	<select id="getChargeListCount" resultType="_int">
		SELECT COUNT(*)
		FROM PAYMENT
		WHERE MEMBER_ID=#{memberId}
	</select>
	<select id="getReceiveListCount" resultType="_int">
		SELECT COUNT(*)
		FROM RESERVE_POINT
		WHERE MEMBER_ID=#{memberId}
	</select>
	<select id="selectChargeList" resultMap="paymentResultSet">
		SELECT PAYMENT_ID, TID, MEMBER_ID, PAY_AMOUNT, PAYMENT_DATE
		FROM PAYMENT
		WHERE MEMBER_ID=#{memberId}
	</select>
	<select id="selectReceiveList" resultMap="reserveResultSet">
		SELECT *
		FROM RESERVE_POINT
		WHERE MEMBER_ID=#{memberId}
	</select>
</mapper>