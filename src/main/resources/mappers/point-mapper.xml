<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="Payment">
	<resultMap type="com.kh.ti.point.model.vo.Payment" id="paymentResultSet">
		<id property="paymentId" column="PAYMENT_ID"/>
		
		<result property="tid" column="TID"/>
		<result property="memberId" column="MEMBER_ID"/>
		<result property="payAmount" column="PAY_AMOUNT"/>
		<result property="paymentDate" column="PAYMENT_DATE"/>
	</resultMap>
	<insert id="insertPay" parameterType="Payment">
		INSERT INTO PAYMENT
		VALUES(PAYMENT_SEQ.NEXTVAL, #{tid}, #{memberId}, #{payAmount}, #{paymentDate})
	</insert>	
	<select id="getChargeListCount" resultType="_int">
		SELECT COUNT(*)
		FROM PAYMENT
		WHERE MEMBER_ID=#{memberId}
	</select>
	<select id="getReceiveListCount" resultType="_int">
		SELECT COUNT(*)
		FROM RESERVE_POINT
		WHERE MEMBER_ID=#{memberId}
	</select>
	<select id="selectChargeList" resultMap="paymentResultSet">
		SELECT *
		FROM PAYMENT
		WHERE MEMBER_ID=#{memberId}
	</select>
</mapper>